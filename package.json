{
  "name": "testing-demo2",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Alex Alonso <alex4551@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:showCoverage": "jest --coverage && open coverage/lcov-report/index.html",
    "test:coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "jest-cli": "^23.0.0"
  },
  "dependencies": {
    "lodash": "^4.17.4"
  },
  "jest": {
    "verbose": true,
    "testURL": "http://localhost/"
  }
}
